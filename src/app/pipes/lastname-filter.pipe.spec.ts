import { LastnameFilterPipe } from './lastname-filter.pipe';

describe('LastnameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
